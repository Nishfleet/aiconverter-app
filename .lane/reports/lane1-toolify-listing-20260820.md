# Lane 1 report — Toolify.ai paid $99 submit path (2026-08-20)

Packet item f8e79adbf9: "List the product on Toolify.ai (paid $99 submit path)
— exact-category tag page already lists LedgerBox and search".

## Outcome: NOT EXECUTED — SKIPPED_PAID stands, and the block is now proved

The listing was **not submitted and the $99 was not paid**. Two independent
blockers, unchanged since 2026-08-11, but the first one is no longer an
inference:

1. **Fleet venue policy gate — executed, exit 4.** Earlier Toolify lanes
   (2026-08-12, 2026-08-15) had to read `venue-policy.json` by hand because
   the `venue-claim` binary was missing from the lane environment. It exists
   now (`/home/nish/.local/bin/venue-claim`, mtime 2026-08-20 15:49), so this
   lane ran the real gate and captured the real refusal (transcript below).
2. **$99 spend is a Nish-only decision (money boundary).**
   `agent-state/authorizations/` contains only
   `sol-xhigh-worker-grant-20260811.json`; there is no Toolify spend grant.
   Toolify's Fulfillment Policy additionally requires account registration
   (a human action) and the fee is non-refundable.

## Guard transcript (the evidence prior lanes could not produce)

```
$ venue-claim check toolify.ai aiconverter-app
policy disposition for toolify.ai: unknown (not reviewed)
exit 0

$ venue-claim claim toolify.ai aiconverter-app \
    --account nishant345+toolify@gmail.com \
    --policy-date 2026-08-20 \
    --policy-url https://www.toolify.ai/fulfillment-policy \
    --copy "AI Converter turns bank statement PDFs into spreadsheet-ready CSV in your browser." \
    --evidence-path .lane/reports/lane1-toolify-listing-20260820.md \
    --removal-route "email Toolify support to request delisting" \
    --verification-state pending
ERROR: ALLOWLIST/POLICY BLOCK: venue toolify.ai is unknown (not allowlisted,
not reviewed). No current official policy evidence permits automated action.
Route to NEEDS-NISH/research.
exit 4

$ venue-claim validate
OK: ledger valid (1 claim(s)), policy valid, registry valid.
executable allowlist: EMPTY - no venue automation-allowed
reviewed (non-executable) venues: alternativeto.net, capterra.com, g2.com,
getapp.com, producthunt.com, startupsubmit.app
exit 0
```

Two details worth recording:

- `check` exits **0** for toolify.ai. That is not permission: `check` only
  looks for a duplicate record and prints the policy disposition to stderr as
  information. `claim` is the gate, and it refuses. An agent that treated
  `check`'s exit 0 as a green light would submit to an unreviewed venue.
- The refusal happens **before** any ledger write — in the source, the
  `entry is None` branch precedes the `with locked():` block — and `validate`
  confirms the ledger still holds exactly the one legacy
  `producthunt.com|seo-fix-kit` record. The attempt left no trace.

## No browser work at the venue this run (deliberate)

`agent-state/growth-loop/venue-claim.md` says `claim` is "called before ANY
venue browser creation/navigation" and "a blocked exit means NO browser work".
The 2026-08-12 and 2026-08-15 Toolify lanes drove Camoufox/Chromium at
toolify.ai *after* concluding the venue was blocked. With the gate now
executable and returning exit 4, this lane honored the block literally: no
browser was opened at toolify.ai, and evidence came from non-browser,
credential-free sources instead. The cost is that this record makes **no
live-page claim** for Toolify; the benefit is that it does not contradict the
guard it is citing.

## Evidence gathered without touching the venue in a browser

| Check | Result |
|---|---|
| Plain curl to `/submit`, `/fulfillment-policy`, `/tool/ai-converter`, `/tag/Bank%20Statement%20to%20CSV` | **HTTP 403** (Cloudflare). The `self-api/v1/best-for-professions` autocomplete endpoint that returned JSON on 2026-08-12 now serves the "Just a moment..." interstitial. No live-page claim is made. |
| `/submit` offer terms (Wayback, latest 200 capture 2026-07-04, `id_` raw fetch) | Unchanged: "Total: $ 99", "Pay $ 99", "No queue, listed within 48 hours", "Just Launched", "no less than 6 quality dofollow links", content modes "Generated by Toolify" / "Do it myself". The kit in `ops/launch-venues.md` still matches the form. |
| `/fulfillment-policy` (Wayback, latest 200 capture 2026-08-11) | Unchanged: "Last updated on August 30, 2024"; "you must register for a paid one-time payment program"; "reject your application for an Account for any reason, in our sole discretion"; fee "non-refundable, even if you cancel or do not use any of the benefits". |
| Automated-access prohibition in that policy capture | **None** — grep for `robot`, `spider`, `crawler` returns zero matches. Still the strongest argument for the research desk to review toolify.ai (unlike Product Hunt's ToS and Toolbit.ai's ToS §7). |
| Duplicate/no-listing check | **Inconclusive this run, and recorded as such.** Wayback CDX has no captures for `/tool/ai-converter` — but it also has none for `/tool/ledgerbox`, a page known to be live, so an empty CDX proves nothing. A DuckDuckGo `site:toolify.ai aiconverter` cross-check hit DDG's bot-anomaly page (HTTP 202, zero results parsed). Last positive readings stand: 2026-08-15 (site search + `/tool/ai-converter` 404) and 2026-08-12 (autocomplete API `{"total": 0, "data": []}`). |
| aiconverter.app kit reference pages (curl) | `/`, `/llms.txt`, `/bank-statement-pdf-to-csv/`, `/sample-csv/`, `/trust/`, `/formats/` → **200**. `/receipt-to-csv/` → 404. `/pricing/` → **404** (see below). |

## Incidental finding: `/pricing/` is 404 in production, and this file said otherwise

`ops/launch-venues.md` opens with "Live-production claims only", and its
"Live-route change (2026-08-12)" section asserted that `/pricing/` now
resolves to a live page. It does not:

```
/pricing            404
/pricing/           404
/pricing/index.html 404
/pricing/ with browser UA + Accept: text/html,...   404   (vary: Accept present)
```

`public/pricing/index.html` is in `main` (commit df944db, "ux: resolve the
Pricing nav to a truthful live /pricing/ page"), so source and production
disagree — a deploy/routing gap rather than a missing page. The 2026-08-15
lane records already noted "/pricing/ still 404" while the top section claimed
the opposite; that contradiction is now resolved in favour of the live
observation, with a dated correction note. No venue kit in the file claims
`/pricing/`, so no kit copy changed. Fixing the deploy gap is outside this
packet and is flagged as an open item.

## Files changed

- `ops/launch-venues.md` — added the dated 2026-08-20 Toolify lane attempt
  (guard transcript, archive evidence, honest inconclusive duplicate check),
  updated the Toolify.ai SKIPPED_PAID status bullet and the verification
  header, and added the dated `/pricing/` correction note.
- `.lane/reports/lane1-toolify-listing-20260820.md` — this report.

## Next action (unchanged, human-owned)

Nish signs in, pays $99, and submits using the kit in `ops/launch-venues.md`
(§ Toolify.ai). An agent could only execute this after **both** (a) Nish's
dated approval of the $99 spend and (b) the venue research desk reviewing
toolify.ai and adding it to the executable allowlist in `venue-policy.json` —
`venue-claim` has no override flag by design. After listing: confirm the tool
on `/tag/Bank%20Statement%20to%20CSV` and that search returns it, then flip
the venue status line to live.
